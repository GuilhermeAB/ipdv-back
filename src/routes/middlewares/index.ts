import isAuthenticated from './auth/is-authenticated';
import isVerified from './auth/is-verified';

export const auth = {
  isAuthenticated: isAuthenticated,
  isVerified: isVerified,
};

export default {};
